import React from 'react'

const headtitle = (props) => {
    return (
        <div>
            <h2 className="App bg-light my-3 py-4">{props.title}</h2>
        </div>
    )
}

export default headtitle
